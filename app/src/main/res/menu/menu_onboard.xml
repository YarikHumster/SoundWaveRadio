<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <item android:id="@+id/navigation_home"
        android:icon="@drawable/ic_home_24dp"
        android:title="@string/menu_home"
        tools:ignore="MenuTitle"/>
    <item android:id="@+id/navigation_categories"
        android:icon="@drawable/ic_category_24dp"
        android:title="@string/menu_categories"
        tools:ignore="MenuTitle"/>
    <item android:id="@+id/navigation_menu"
        android:icon="@drawable/icon_menu"
        android:title="@string/menu_general"
        tools:ignore="MenuTitle"/>
</menu>